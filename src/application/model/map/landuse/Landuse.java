package application.model.map.landuse;

import application.model.Colors;
import application.model.map.Area;

public class Landuse extends Area {

	public Landuse(long id, String name, String color, String colorBord) {
		super(id, name, color, colorBord);
		// TODO Auto-generated constructor stub
	}

}
